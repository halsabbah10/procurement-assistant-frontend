# Procurement Assistant — Frontend

React + TypeScript chat UI and live analytics dashboard for the Procurement Assistant.

Backend repo: (link added here once both repos are pushed — see Task 21)

## Quick start

Requires the backend running separately (see the backend repo's README for its
own `docker-compose up`).

```bash
cp .env.example .env   # set VITE_API_BASE_URL to the backend's URL
npm install
npm run dev
```

http://localhost:5173

## Commands

See `CLAUDE.md` for the full command reference.
